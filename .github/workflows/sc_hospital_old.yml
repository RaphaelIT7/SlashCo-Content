name: sc_hospital_old

on:
  workflow_dispatch:
  push:
    paths:
      - 'maps/sc_hospital_old/**'
      - '.github/workflows/sc_hospital_old.yml'

jobs:
  compile:
    uses: RaphaelIT7/SlashCo-Content/.github/workflows/map_compiler.yml@main
    secrets: inherit
    with:
      fastcompile: false
      createexportbsp: false
      debug: false
      vrad_mode: "-ldr"
      map: "sc_hospital_old"
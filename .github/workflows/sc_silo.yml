name: sc_silo

on:
  workflow_dispatch:
  push:
    paths:
      - 'maps/sc_silo/**'
      - '.github/workflows/sc_silo.yml'

jobs:
  compile:
    uses: RaphaelIT7/SlashCo-Content/.github/workflows/map_compiler.yml@main
    secrets: inherit
    with:
      fastcompile: false
      createexportbsp: false
      debug: false
      map: "sc_silo"
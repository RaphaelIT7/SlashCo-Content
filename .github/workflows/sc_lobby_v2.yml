name: sc_lobby_v2

on:
  workflow_dispatch:
  push:
    paths:
      - 'maps/sc_lobby_v2/**'

jobs:
  compile:
    uses: RaphaelIT7/SlashCo-Content/.github/workflows/map_compiler.yml@main
    secrets: inherit
    with:
      fastcompile: false
      createexportbsp: false
      debug: false
      map: "sc_lobby_v2"
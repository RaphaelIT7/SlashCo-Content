name: sc_marsh_night

on:
  workflow_dispatch:
  push:
    paths:
      - 'maps/sc_marsh_night/**'
      - '.github/workflows/sc_marsh_night.yml'

jobs:
  compile:
    uses: RaphaelIT7/SlashCo-Content/.github/workflows/map_compiler.yml@main
    secrets: inherit
    with:
      fastcompile: false
      createexportbsp: true
      debug: false
      vrad_mode: "-both"
      map: "sc_marsh_night"
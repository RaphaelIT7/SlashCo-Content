name: sc_autumnpark

on:
  workflow_dispatch:
  push:
    paths:
      - 'maps/sc_autumnpark/**'
      - '.github/workflows/sc_autumnpark.yml'

jobs:
  compile:
    uses: RaphaelIT7/SlashCo-Content/.github/workflows/map_compiler.yml@main
    secrets: inherit
    with:
      fastcompile: false
      createexportbsp: true
      debug: false
      vrad_mode: "-both"
      map: "sc_autumnpark"
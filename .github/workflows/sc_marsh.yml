name: sc_marsh

on:
  workflow_dispatch:
  push:
    paths:
      - 'maps/sc_marsh/**'
      - '.github/workflows/sc_marsh.yml'

jobs:
  compile:
    uses: RaphaelIT7/SlashCo-Content/.github/workflows/map_compiler.yml@main
    secrets: inherit
    with:
      fastcompile: false
      createexportbsp: true
      debug: false
      vrad_mode: "-ldr"
      map: "sc_marsh"